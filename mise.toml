[tools]
tuist = "4.191.4"

[tasks.auth]
description = "Login to Tuist account"
run = "tuist auth login"

[tasks.edit]
description = "Edit manifest"
run = "tuist edit"

[tasks.install]
description = "Run Tuist install"
run = "tuist install"

[tasks.setupXcodeCache]
description = "Run Tuist setup cache"
run = "tuist setup cache"

[tasks.generate]
description = "Generate the Xcode project"
alias = "g"
run = "tuist generate"

[tasks.test]
description = "Run tests"
run = "tuist test"

[tasks."Inspect implicit dependencies"]
run = "tuist inspect dependencies --only implicit"

[tasks.lint]
run = "swift-format . --recursive --in-place"

[tasks.setup]
description = "Install and generate"
run = [
    { task = "install" },
    { task = "generate" },
]
